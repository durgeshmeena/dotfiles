## default creds
#User user
#IdentityFile ~/.ssh/id_rsa

# scp
# use scp with -O flag to disable security popup
# scp -O ./{etcd,etcdctl,etcdutl} user@host:/path/to/file

Host *
    ## Store keys in the client when first using them.
    #AddKeysToAgent yes

    # Make all sessions to the same host share a single connection.
    ControlMaster auto
    ControlPersist 120
    ControlPath ~/.ssh/sockets/socket-%r@%h:%p